import AnalogClock from './AnalogClock.js';
import tickTock from './tickTock.js';

document.querySelectorAll('.analog-clock').forEach(AnalogClock);
document.querySelectorAll('.analog-clock').forEach(tickTock);